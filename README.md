# tddnnf
A Python framework for Knowledge Compilation Modulo Theories

## Setup

```bash
# Install dependencies
uv sync

# Install Mathsat solver (required for normalization)
uv run pysmt-install --msat
```
